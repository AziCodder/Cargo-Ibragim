{
  "name": "cargo-tracking",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run backend\" \"npm run frontend\" \"npm run bot\"",
    "dev:full": "concurrently \"npm run backend\" \"npm run frontend\" \"npm run bot\"",
    "backend": "python -m uvicorn backend.main:app --reload --host 0.0.0.0 --port 8800",
    "frontend": "cd frontend && npm run dev",
    "bot": "python telegram_bot/bot.py"
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}
